package hashcode_2022

import core.ProblemInput
import core.ProblemOutput

class Solver() {

    fun solve(input: ProblemInput): ProblemOutput {
        //SOLVE THE PROBLEM

        return ProblemOutput(input.data)
    }
}
